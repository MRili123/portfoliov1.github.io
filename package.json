{
  "name": "my-three-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "deploy": "gh-pages -d dist -rhttps://github.com/MRili123/portfoliov1.github.io.git",
    "dev": "node server",
    "build": "vite build",
    "build:client": "vite build --outDir dist/client",
    "build:server": "vite build --ssr src/entry-server.js --outDir dist/server",
    "preview": "cross-env NODE_ENV=production node server"
  },
  "dependencies": {
    "compression": "^1.7.5",
    "express": "^5.0.1",
    "sirv": "^3.0.0",
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.2.0",
    "vite": "^6.0.4"
  }
}
